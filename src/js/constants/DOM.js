const popupSigninActive = document.querySelector('.popup__signin_active');

const popupSignupActive = document.querySelector('.popup__signup_active');

const popupSignupForm = popupSignupActive.querySelector('.popup__form');

const popupSigninForm = popupSigninActive.querySelector('.popup__form');

const searchForm = document.querySelector('.search__form');

const headerUser = document.querySelector('.header__user');

const headerAutho = document.querySelector('.header__autho');

const headerName = document.querySelector('.header__name');

const articlesList = document.querySelector('.articles-list');

const preloader = document.querySelector('.preloader');

const articles = document.querySelector('.articles');

const notFound = document.querySelector('.not-found');

const articlesButton = document.querySelector('.articles__button');

const popupOpen = document.querySelector('.popup__open_active');

const popupOpen2 = document.querySelector('.popup__open2_active');

const popupSuccessActive = document.querySelector('.popup__success_active');

export {
  popupSigninActive,
  popupSignupActive,
  popupSigninForm,
  popupSignupForm,
  searchForm,
  headerUser,
  headerAutho,
  headerName,
  articlesList,
  preloader,
  articles,
  notFound,
  articlesButton,
  popupOpen,
  popupOpen2,
  popupSuccessActive,
};
